# Python dependencies (Python 3.10+)
numpy>=1.24
pandas>=2.0
torch>=2.1.0

# External tools (install separately, not via pip):
#   - MMseqs2 v18+   for code/data_preprocessing/ (sequence similarity reduction)
#   - HMMER 3.x      for code/common/eval_hmmer.py (see code/common/setup_hmmer.sh)
#   - IgFold         for code/common/eval_foldability.py (see common/README.md)
