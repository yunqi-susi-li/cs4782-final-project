# Core
torch>=2.1.0
numpy>=1.24
pandas>=2.0
tqdm>=4.65
pyyaml>=6.0
einops>=0.7.0

# Protein LM / ESM baseline
fair-esm>=2.0.0
transformers>=4.40.0     # only used to load ESM-2 weights; LoRA is self-implemented
biopython>=1.81

# Alignment (Needleman-Wunsch for germline edit track)
parasail>=1.3            # optional; fallback pure-python impl in code/common/alignment.py

# Data / immune-receptor utilities
pyarrow>=14.0            # read OAS parquet
pyir>=1.5                # V(D)J annotation (optional; OAS ships pre-annotated)

# Training / logging
wandb>=0.16              # optional
matplotlib>=3.7
seaborn>=0.13

# Dev
pytest>=7.4
ruff>=0.3
